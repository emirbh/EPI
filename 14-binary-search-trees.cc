#include <iostream>
#include <limits>

using std::cout;
using std::endl;

#include "idebug.h"
#include "binary-search-trees.h"

int main() {
  int part;
  cin >> part;
  cin.ignore(numeric_limits<streamsize>::max(), '\n');

  while(part != -1) {
    cout << "Part: " << part << "----------------------------------------";
    cout << "--------------------------------" << endl;

    switch(part) {
      /* ---- 02 ------------------------------------------------------------ */
      case 2: {
        break;
      }
      /* ---- 04 ------------------------------------------------------------ */
      case 4: {
        break;
      }
      default: {
        break;
      }
    }

    cin >> part;
  }
}
